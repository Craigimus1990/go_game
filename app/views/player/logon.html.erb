<% if flash[:notice] %>
	<div class="notice"><%= flash[:notice] %></div>
<% end %>

<%= form_tag do %>
	<%= label_tag(:player, "Login as...") %>
	<br>
	<%= select_tag(:player_id, options_from_collection_for_select(Player.all, :id, :name)) %>
	<br>
  <br>
  <%= submit_tag("Submit") %>
<% end %>

